# Flet - 现代化Python UI框架
flet>=0.28.3

# 音频处理
PyAudioWPatch>=0.2.12.7  # Windows WASAPI 支持的 PyAudio 分支（优先）
pyaudio>=0.2.14          # 标准 PyAudio 后备
numpy>=1.21.0
scipy>=1.9.0
sounddevice>=0.5.0       # 备用音频库，解决Windows兼容性问题

# FunASR 语音识别依赖
funasr>=1.2.7
modelscope>=1.9.0
torch>=2.0.0
torchaudio>=2.0.0
librosa>=0.10.0

# FunASR 必需的额外依赖
omegaconf>=2.3.0
torch-complex>=0.4.3
hydra-core>=1.3.0

# 其他系统依赖
requests>=2.28.0
pyyaml>=6.0